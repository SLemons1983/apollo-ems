import { redirect } from 'next/navigation';
import { currentEpcrMembership } from '@/lib/epcrServer';
export default async function EpcrDashboard(){const access=await currentEpcrMembership();if(!access)redirect('/epcr/login');const m=access.membership;const agency=Array.isArray(m.apollo_agencies)?m.apollo_agencies[0]:m.apollo_agencies;return <main className="min-h-screen bg-slate-100 p-6"><div className="mx-auto max-w-6xl"><p className="font-bold text-blue-700">{agency?.name ?? 'Agency ePCR'}</p><h1 className="mt-2 text-4xl font-black">Welcome, {m.first_name}</h1><p className="mt-2 text-slate-600">{m.role.replaceAll('_',' ')} · @{m.username}</p><section className="mt-8 grid gap-5 md:grid-cols-3"><a href="/ePCR" className="rounded-3xl bg-blue-700 p-7 text-white shadow"><b className="text-xl">Start new ePCR</b><p className="mt-2 text-blue-100">Open the current report editor.</p></a><div className="rounded-3xl bg-white p-7 shadow"><b className="text-xl">My reports</b><p className="mt-2 text-slate-600">Report saving and submission arrive in the next controlled release.</p></div>{m.role!=='CLINICIAN'&&<div className="rounded-3xl bg-white p-7 shadow"><b className="text-xl">Review queue</b><p className="mt-2 text-slate-600">Correction, completion, and feedback workflow arrives after secure report storage.</p></div>}</section></div></main>}
